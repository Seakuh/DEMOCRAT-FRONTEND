# Optional: DIP Bundestag API Key
# If not set, the app will use dev proxy or fall back to mock data
# VITE_DIP_API_KEY=your-api-key-here

# Optional: DIP Base URL (default: /dip for dev proxy)
# VITE_DIP_BASE_URL=/dip
